Smart Elevator Control
Web Dev Cohort 2026

Databases

Timeline
Start: 10 Apr 2026, 9:45 am
Due: 10 May 2026, 11:59 pm
Eval Begins: 11 May 2026, 12:00 am
Eval Ends: 13 May 2026, 11:59 pm
Instructions
A fast-growing infrastructure technology company called LiftGrid Systems builds intelligent elevator control platforms for large commercial buildings across India.

Their software is used in corporate towers, malls, airports, hospitals and high-rise residential complexes where dozens of elevators operate together across many floors.

Unlike small standalone lifts, these buildings run multiple elevators per building, grouped into zones, handling thousands of passengers daily. The system must manage elevator assignments, floor requests, maintenance tracking and ride logs efficiently.

Each building can contain multiple elevator shafts. Each shaft contains one elevator. Each elevator moves across a defined set of floors and responds to ride requests generated by users from different floors.

The system should support:

multiple buildings

multiple elevators inside each building

floor-level request tracking

ride allocation to elevators

elevator status monitoring

maintenance tracking

usage history logging

This backend platform helps operations teams monitor performance and ensures elevators remain safe, efficient and available.

Your task is to design the ER diagram for this smart elevator control system.

This is a multi-building infrastructure management system handling real-time movement requests, elevator allocation and operational tracking.

What You Have to Do
Your design should support questions like:

How many buildings are connected to the platform?

How many elevators exist inside a building?

Which floors belong to which building?

Which elevator serves which floors?

What requests were generated from which floors?

Which elevator handled a request?

Can multiple elevators serve the same floor?

Can one elevator serve multiple floors?

What is the status of each elevator (idle, moving, maintenance)?

How many rides did an elevator complete today?

Which elevator handled the most requests?

Which requests are still pending?

Can an elevator be temporarily disabled for maintenance?

Can maintenance history be tracked per elevator?

Can ride logs be recorded for analytics later?

What to Make
Design the ER diagram for this smart elevator control platform.

Your ERD should include structures needed for:

buildings

floors

elevators

elevator shafts (optional but recommended)

floor requests

ride assignments

ride logs or trips

elevator status tracking

maintenance tracking

Things to think about:

one building contains many floors

one building contains many elevators

elevators belong to buildings

elevators serve multiple floors

floors can be served by multiple elevators

users generate floor requests

requests are assigned to elevators

one request results in one ride allocation

elevators complete many rides daily

maintenance should not overwrite elevator history

status should not be mixed with configuration data

avoid storing dynamic ride data inside elevator entity

You do not need to write SQL queries
You do not need to build APIs
This task is only about database design

Submission Instructions
Submit any one of the following:

an image export of your ER diagram
a PDF export of your ER diagram
an Excalidraw / Eraser / Draw.io / FigJam board link
a GitHub repository link containing the diagram file or exported image
Submission expectations:

one single board is preferred
the diagram must be readable
entity names and attribute names should be clearly visible
PK and FK should be clearly marked
relationship lines should not look random or confusing
if you use GitHub, keep the diagram inside the repo properly and include a short README
Evaluation Parameters
Infrastructure Understanding (15 marks)
Did the student understand that buildings contain floors and elevators separately?

Did they separate elevator configuration from ride activity data?

Entity Quality (20 marks)
Did they correctly model building, floor, elevator, request, ride assignment and maintenance entities?

Did they avoid merging requests directly into elevators?

Relationship Modeling (20 marks)
Are building–floor relationships modeled correctly?

Are building–elevator relationships modeled correctly?

Is request-to-elevator assignment modeled properly?

Are many-to-many relations handled where required?

Attribute and Table Design (15 marks)
Are static structures separated from dynamic ride logs?

Is elevator status handled separately from ride history?

Are maintenance details stored properly instead of mixing inside elevator tables?

PK/FK and Junction Tables (15 marks)
Did the student correctly connect requests with elevators?

Did they properly model floor–elevator servicing relationships?

Are ride assignment tables designed correctly?

Real-World Practicality (10 marks)
Does this feel like software that could run inside a real high-rise infrastructure monitoring system?
Diagram Neatness (5 marks)
Is the ER diagram clean and readable?

Are relationships clearly labeled?

-----------


buildings [icon: menu, color: red] {
    id int autoincrement pk
    building_name VARCHAR
    building_type VARCHAR
    address TEXT
    city VARCHAR
    total_floors INT
    created_at timestamp not null
    updated_at timestamp not null
}

floors [icon: menu, color: red] {
    id int autoincrement pk
    building_id INT FK
    floor_number INT
    floor_name VARCHAR
    zone VARCHAR
    created_at timestamp not null
    updated_at timestamp not null
}

elevatorShafts [icon: menu, color: red] {
    id int autoincrement pk
    building_id INT FK
    shaft_code VARCHAR
    location_description TEXT
    created_at timestamp not null
    updated_at timestamp not null
}

elevators [icon: menu, color: red] {
    id int autoincrement pk
    building_id INT FK
    shaft_id INT FK
    elevator_code VARCHAR
    manufacturer VARCHAR
    capacity INT
    installation_date DATE
    max_speed DECIMAL
    current_floor_id INT FK
    is_active BOOLEAN
    created_at timestamp not null
    updated_at timestamp not null
}

elevatorFloorMap [icon: menu, color: red] {
    id int autoincrement pk
    elevator_id INT FK
    floor_id INT FK
}

floorRequests [icon: menu, color: red] {
    id int autoincrement pk
    building_id INT FK
    source_floor_id INT FK
    destination_floor_id INT FK
    request_time DATETIME
    direction ENUM(UP,DOWN)
    request_status ENUM(Pending,Assigned,Completed,Cancelled)
    created_at timestamp not null
    updated_at timestamp not null
}

rideAssignments [icon: menu, color: red] {
    id int autoincrement pk
    request_id INT FK
    elevator_id INT FK
    assigned_at DATETIME
    assignment_status ENUM
    created_at timestamp not null
    updated_at timestamp not null
}

rideLogs [icon: menu, color: red] {
    id int autoincrement pk
    elevator_id INT FK
    request_id INT FK
    start_floor_id INT FK
    end_floor_id INT FK
    ride_start_time DATETIME
    ride_end_time DATETIME
    duration_seconds INT
    created_at timestamp not null
    updated_at timestamp not null
}

elevatorStatuses [icon: menu, color: red] {
    id int autoincrement pk
    elevator_id INT FK
    current_floor_id INT FK
    operational_status ENUM(Idle,Moving,Maintenance,Offline)
    direction ENUM(Up,Down,Stationary)
    created_at timestamp not null
    updated_at timestamp not null
}

maintainanceRecords [icon: menu, color: red] {
    id int autoincrement pk
    elevator_id INT FK
    maintenance_type VARCHAR
    issue_reported TEXT
    maintenance_start DATETIME
    maintenance_end DATETIME
    technician_name VARCHAR
    maintenance_status ENUM(Scheduled,InProgress,Completed)
    created_at timestamp not null
    updated_at timestamp not null
}

buildings.id < floors.building_id
buildings.id < elevators.building_id
elevatorShafts.id - elevators.shaft_id
elevators.id < elevatorFloorMap.elevator_id
floors.id < elevatorFloorMap.floor_id
buildings.id < floorRequests.building_id
floors.id < floorRequests.source_floor_id
floors.id < floorRequests.destination_floor_id
floorRequests.id < rideAssignments.request_id
elevators.id < rideAssignments.elevator_id
elevators.id < rideLogs.elevator_id
floorRequests.id < rideLogs.request_id
floorRequests.id < rideLogs.start_floor_id
floorRequests.id < rideLogs.end_floor_id
elevators.id < elevatorStatuses.elevator_id
floors.id < elevatorStatuses.floor_id
elevators.id < maintainanceRecords.elevator_id