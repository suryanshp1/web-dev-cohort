version: '3.8'
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: auth0_mvp_user
      POSTGRES_PASSWORD: auth0_mvp_password
      POSTGRES_DB: auth0_mvp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
